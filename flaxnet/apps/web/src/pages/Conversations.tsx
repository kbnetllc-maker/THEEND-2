export default function Conversations() {
  return (
    <div>
      <h1 className="text-2xl font-semibold text-white">Conversations</h1>
      <p className="mt-2 text-slate-400">Inbox + SMS thread — Twilio webhook + polling next.</p>
    </div>
  );
}
