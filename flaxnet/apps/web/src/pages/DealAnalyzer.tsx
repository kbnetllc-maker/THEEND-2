export default function DealAnalyzer() {
  return (
    <div>
      <h1 className="text-2xl font-semibold text-white">Deal analyzer</h1>
      <p className="mt-2 text-slate-400">MAO calculator + AI risk — use /api/deals + /api/ai/analyze-deal.</p>
    </div>
  );
}
