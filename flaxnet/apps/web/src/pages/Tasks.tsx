export default function Tasks() {
  return (
    <div>
      <h1 className="text-2xl font-semibold text-white">Tasks</h1>
      <p className="mt-2 text-slate-400">Today / upcoming / overdue — optimistic PATCH next.</p>
    </div>
  );
}
