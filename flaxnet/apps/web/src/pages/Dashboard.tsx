export default function Dashboard() {
  return (
    <div>
      <h1 className="text-2xl font-semibold text-white">Dashboard</h1>
      <p className="mt-2 text-slate-400">
        Wholesaler command center — wire KPIs, hot leads, and import status (MVP shell).
      </p>
    </div>
  );
}
